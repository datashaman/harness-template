{
  "name": "typescript",
  "description": "TypeScript profile: tsc, eslint v9 flat config, prettier, vitest, knip. Policy via system semgrep (pip install semgrep).",
  "sensors": {
    "format": "npx prettier --check .",
    "lint": "npx eslint .",
    "typecheck": "npx tsc --noEmit",
    "test": "npx vitest run --coverage",
    "deadcode": "npx knip",
    "audit": "npm audit --audit-level=high",
    "policy": "command -v semgrep >/dev/null && semgrep --config harness/policies/ --error || echo 'semgrep not installed (pip install semgrep) — skipping policy sensor' >&2"
  }
}
